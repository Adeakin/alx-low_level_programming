#include <stdio.h>
#include <ctype.h>
/**
 * main - to print all single numbers of base 10
 *
 * Return: 0, if succesful
 */
int main(void)
{
	int a;

	for (a = 0 ; a < 10 ; a++)
	printf("%d", a);
	printf("\n");
	return (0);
}
